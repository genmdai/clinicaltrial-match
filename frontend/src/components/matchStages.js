// Fixed narration stages for the /match pipeline — must stay in sync with the
// exact strings the backend yields as `progress` events in backend/main.py.
export const MATCH_STAGES = [
  'Searching recruiting trials…',
  'Checking recruiting sites…',
  'Comparing eligibility criteria…',
  'Looking for hospital access information…',
  'Finalizing results…',
]
